# --- CONFIGURACIÓN DEL BACKEND ---
BACK_PORT=4000
JWT_SECRET=tu_secreto_super_seguro_123

# --- CONFIGURACIÓN DE LA BASE DE DATOS (PostgreSQL) ---
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=gestion_tiempo
DB_HOST=db
DB_PORT=5432

# --- CONFIGURACIÓN DEL FRONTEND ---
FRONT_PORT=3000

# --- CONFIGURACIÓN CORREO (PRODUCCIÓN) ---
NODE_ENV=production
SMTP_HOST=smtp.proveedor.com
SMTP_PORT=587
SMTP_USER=usuario_smtp
SMTP_PASS=clave_smtp
SMTP_FROM=no-reply@tudominio.com
