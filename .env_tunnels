# --- CONFIGURACIÓN DEL BACKEND ---
BACK_PORT=4000
JWT_SECRET=tu_secreto_super_seguro_123
NODE_ENV=development
BACK_ENV=development

# --- CONFIGURACIÓN DE LA BASE DE DATOS (PostgreSQL) ---
DB_USER=gestion_user
DB_PASSWORD=gestion_pass
DB_NAME=gestion_tiempo
DB_HOST=db
DB_PORT=5432
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=false
DB_POOL_MAX=10
DB_POOL_MIN=0
DB_POOL_IDLE_TIMEOUT_MS=30000
DB_POOL_CONNECTION_TIMEOUT_MS=2000
DB_POOL_KEEPALIVE=true

# --- CORS (agrega la URL pública del FRONT) ---
# Ejemplo: https://xxxx-3000.use2.devtunnels.ms
CORS_ORIGINS=https://d053flnv-3000.use.devtunnels.ms


# --- REFERENCIAS PARA TÚNeles (FRONT) ---
# API_BASE se usa en front/env.js (edita ese archivo también).
API_BASE=
FRONT_PORTAL_BASE=https://zealous-mud-057b4ca0f.1.azurestaticapps.net/index.html
